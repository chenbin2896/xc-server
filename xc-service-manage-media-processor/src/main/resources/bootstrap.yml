spring:
  application:
    name: xc‐service‐manage‐cms‐client
  cloud:
    nacos:
      discovery:
        server-addr: docker-base:8848
      config:
        server-addr: docker-base:8848
        file-extension: yaml