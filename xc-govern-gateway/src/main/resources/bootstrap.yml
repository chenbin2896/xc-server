spring:
  application:
    name: xc-govern-gateway
  cloud:
    nacos:
      discovery:
        server-addr: docker-base:8848
      config:
        server-addr: docker-base:8848
        file-extension: yaml
whitelist:
  include:
    - /api/ucenter/getuserext
    - /api/ucenter/getUserById
    - /api/auth/login